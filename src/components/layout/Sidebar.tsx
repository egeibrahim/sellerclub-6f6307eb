// Legacy Sidebar - now replaced by IconSidebar + ShopFilterSidebar in Layout
// This file is kept for backwards compatibility with routes that don't use the new layout

import { IconSidebar } from "./IconSidebar";

export function Sidebar() {
  return <IconSidebar />;
}
